{
  "workspaces": [
    "examples/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "format:check": "turbo run format:check",
    "jest": "turbo run jest",
    "test": "turbo run lint typecheck format:check jest",
    "clean": "yarn workspaces run clean",
    "build": "turbo run build"
  },
  "dependencies": {},
  "devDependencies": {
    "turbo": "^1.3.1"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all",
    "arrowParens": "always"
  },
  "license": "ISC",
  "private": true
}
